/** @format */

import { Typography } from "@mui/material";

const Settings = () => {
  return (
    <div>
      <Typography variant="h4">Admin Settings</Typography>
      <Typography>Only admin can see this page.</Typography>
    </div>
  );
};

export default Settings;
